#ifndef ITEMACTIONSMENU_H
#define ITEMACTIONSMENU_H

#include "../common.h"

void scrItemActionsMenu(Game *g);
void updateItemActionsMenu(Game *g);
void drawItemActionsMenu(Game *g);
void checkItemActionsMenu(Game *g);

#endif